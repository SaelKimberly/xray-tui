# DRY normalization: dedupe semantically identical entities across workspace

## Context

User asks to detect semantically identical entities across the xray-tui workspace and normalize the codebase for DRY. Three read-only audits (proto, native+tls, app+db) found ~4,700 duplicated LOC in 30+ clusters — per-protocol copy-paste families, twin decoders split across crates, repeated TUI/ops skeletons. This plan consolidates them into shared helpers/macros/modules while pinning behavior with existing tests. Exclusions (deliberate): machine-generated code (`xray-tui-tls/src/fingerprints/catalog/catalog_data.rs` via gen.py, `tls/src/profiles/generated/*` via gen_specs.py), `xray-tui-hakari` (generated feature unification), `thirdparty/*` (read-only references), `native/src/crypto/b3.rs` (justified single-site BLAKE3 reimplementation), and xray-vs-singbox config builders' mirrored JSON dialects (intentional port; shared atoms already factored).

## Approach

Execute as four waves. Within a wave steps are independent; waves are sequential — each must leave `cargo test -p <wave-crates>` green before next starts.

### Wave 1 — proto crate (~2.5k LOC)

1. **Delete legacy `ProtoSpec` bridge trait + its 20 impl blocks.** In `crates/xray-tui-proto/src/proto_spec/mod.rs` remove the `pub trait ProtoSpec` definition (~lines 296-361), the 20 per-config `impl ProtoSpec for XConfig` blocks (one per config file; find exact set with `grep -rn "impl ProtoSpec" crates/xray-tui-proto/src`), and the ProtoSpec-bridging inherent methods on `ProtocolConfig` that exist only to serve it (`schema`, `host`, `port`, `remarks`, `country_flags`, `to_json_config`, `reconstruct`, `try_from_clash` — keep everything routed through `dispatch!` that has real callers; verify each with `grep`). Migrate every external callsite found by `grep -rn "ProtoSpec\b\|::try_parse(\|\.reconstruct()\b\|\.schema()\|\.country_flags()\|\.security()\|\.transport_type()" crates/xray-tui/src crates/xray-tui-config/src crates/xray-tui-core/src crates/xray-tui-native/src --include='*.rs' | grep -v _proto | grep -v 'fn '`. Known callers to migrate: `crates/xray-tui/src/ops/enrich.rs:23-32` (`extract_sni` uses trait `.security()` on `config.get().0` — replace with typed field access mirroring line 29's fallback: `protocol.security.sni.clone()` path already exists, so make the loaded-config branch read `SecurityConfig::sni()` directly off the typed struct without the trait import); `crates/xray-tui/src/lib.rs:27` and `crates/xray-tui/src/state.rs:23` (imports only — delete `ProtoSpec` from use lists); `crates/xray-tui-config/src/import_export.rs:675`, `crates/xray-tui-config/src/subscription.rs:10`, `crates/xray-tui-native/src/e2e/case.rs:552`, `crates/xray-tui-native/src/e2e/config.rs:794` (test imports — switch to direct `*_proto` methods). Keep the private `ProtoIdentity` supertrait and its dispatch impls — identity hashing is live.
2. **Add `singbox_out_base()` + shared inject helpers** in `crates/xray-tui-proto/src/proto_spec/common.rs`: `pub(crate) fn singbox_out_base(kind_tag: &'static str, endpoint: &EndpointEssentials) -> serde_json::Value` returning `{tag:"proxy", type:<kind>, server, server_port}`; signature mirrors existing `to_singbox_tls_or_default` at common.rs:1034. Migrate all 14 `inject_singbox` bodies (tuic.rs:431 is canonical example) to call it then attach protocol fields; also replace the `let Some(ep) = endpoint else { return Err(SupportError::MissingField("server", "<proto>")) }` unwrap idiom with `fn singbox_endpoint(endpoint: Option<&EndpointEssentials>) -> Result<&EndpointEssentials, SupportError>` taking the kind tag (51 MissingField sites crate-wide are covered by this + step 1). No behavior change: same JSON keys, same error literals (`SupportError::MissingField("server", "tuic")` etc.) — tests asserting these error shapes stay green.
3. **Add URL-serializer builder** in common.rs: `pub(crate) struct ShareUrlCtx { parts: Vec<String> }` with `hostport(endpoint)->String` (IPv6-bracket logic, replaces 13 byte-identical `if contains(':')` blocks), `push_opt(key, Option<&str>)` emitting `k=<urlencoding::encode(v)>` when Some and not `should_skip_endpoint_param(host, v)` for host-bearing keys, `finish_query()->String` producing `""` or `"?a=b&c=d"`, `fragment(Option<&str>)->String`. Replace query assembly in all 13 `reconstruct_proto` impls (trojan.rs:211 is the heaviest template; ssh/tor/tailscale error stubs untouched). Existing round-trip parse/reconstruct tests per protocol file are the pin.
4. **Collapse reconstruct-side duplication**: extract `security_query_pairs(&SecurityConfig, endpoint_host) -> Vec<(String,String)>` (sni/alpn/fp/insecure triples ×4 + reality pbk/sid/spx ×2, feeding ShareUrlCtx) and `transport_host_param(&TransportConfig) -> Option<&str>` (the Ws/Grpc/Http/HttpUpgrade/XHttp `cfg.host|authority` match ×7) into common.rs; wire into step 3's migration so each `reconstruct_proto` shrinks to userinfo scheme + table of pushes. Add `fn parsed_proto(proto_kind, config_type, core_type, config, endpoints) -> ParsedProto` free ctor in `proto_spec/mod.rs` replacing the 24 near-identical `Ok(ParsedProto{...})` tails, plus `remarks_from_name(Option<&str>) -> Option<String>` replacing the 17 copies of the `""=>None` match.
5. **Parse-prelude helper**: add `split_userinfo_hostport(raw) -> Result<(Option<String>, &str parsed_host, u16 port), ParseError>` + `endpoint_from_spec(parsed_host, parsed_port, ports_iter) -> EndpointEssentials` in `utils.rs`, migrating the 12 try_parse preludes (tuic.rs:110-113 pattern is canonical).
6. **Kind-enum plumbing macro** in `kinds.rs`: one `macro_rules! kind_str_plumbing { ... }` generating `as_str`/`Display`/`Serialize`/`Deserialize`/`FromStr` per enum from a single variant→string table; apply to ProtocolKind/TransportType/SecurityType/CoreType (their string tables move into the single source; `as_str_equals_protocol_display_strings` test at kinds.rs:437 pins this and must stay green). Also collapse `ProtocolKind::try_from_i32/to_i32` mirrored 27-arm tables onto the same table where the i32 mapping is contiguous-derived; if any arm's i32 is irregular, leave i32 tables hand-written (do not invent a formula).
7. **ProtoIdentity macro**: `macro_rules! proto_identity` generating compute_sig bodies (rapidhash init/tag/finish scaffolding ×20); compute_cred_hash stays as-is (already centralized in utils.rs).
8. **Canonical base64/percent decoders**: promote `xray-tui-config/src/base64_util.rs::decode_base64` algorithm to `xray-tui-proto/src/proto_spec/utils.rs` (keep the existing proto-side engine = plain `base64` crate + `urlencoding`, not `base64_simd` — proto has no simd dep; merge the percent-decode-first behavior from base64_util which proto currently lacks AFTER confirming proto consumers ss/ssr/vmess don't depend on missing it: their inputs come from RawUrlX which pre-splits and percent-decodes, so added percent-decode is a no-op there). Delete `base64_util.rs`; migrate its callers (`grep -rn "base64_util" crates --include='*.rs'` → only xray-tui-config internal today; subscription.rs streaming decoder is separate machinery, untouched). Delete `import_export.rs:369 percent_decode` + `hex_val_sub` + `fix_percent_encoding` (:350) in favor of `urlencoding::decode` with the %25-repair folded into the new shared `percent_decode_str`. Keep `fast_perc.rs` (intentional hot-path SIMD specialization) and keep `coerce_bool` spellings exactly — do NOT migrate the strict `"1"|"true"|"True"` insecure parsers onto loose `coerce_bool` ("yes"/"on"/"y"/"n"/"off" spellings would change parse behavior; instead add `fn coerce_flag_str(s:&str)->Option<bool>` matching `"1"|"true"|"True"|"yes"` true / `"0"|"false"|"False"|"no"` false beside coerce_bool in utils.rs and migrate the 6 inline copies at anytls.rs:104, http_client.rs:121, hysteria1.rs:144, hysteria2.rs:152 + siblings).
9. Fresh-environment gate: `cargo build -p xray-tui-proto && cargo nextest run -p xray-tui-proto -p xray-tui-config`.

### Wave 2 — native + tls (~800 LOC)

1. **XHTTP dialect fold**: in `crates/xray-tui-native/src/transport/xhttp.rs` replace 9 near-identical fns packet_up/stream_up/stream_one × h1/h2/h3 (:924,:952,:971,:994,:1016,:1037,:1066,:1083,:1099) with generic fns parametrized over the existing `V3Send` trait + dial seam + Mode enum; upload_loop wrappers :619/:630/:642 follow. e2e xhttp tests pin wire shape.
2. **One ChannelDuplex**: extract XhttpStream (:1127) vs V2rayStream (v2rayhttp.rs:134) into one generic channel-backed duplex stream module (shared kick/poll_flushing/poll_write/flush/shutdown + PendingSend alias); both call sites become thin type aliases/wrappers keeping public names stable.
3. **Policy-surface completion**: add `is_reality()` and `http_version()` accessors to `LinkContext` (`context.rs`) reading existing context data; migrate the two bypass sites (xhttp.rs:655 is_reality leak; xhttp http_version decision) to read from context per AGENTS LinkContext mandate.
4. **chain.rs fold**: unify connect_chain/_udp/_mux (:46,:69,:96) loop skeleton into one phase-parametrized walk; three public fns remain as thin wrappers preserving signatures.
5. **Small utils**: single `write_all_timeout` (already exists privately in vless/encryption/mlkem.rs — move to a shared native mod, migrate 8 copies); single decode_hex exported from `xray-tui-tls/src/profiles/mod.rs:33` replacing 9 test-helper copies across both crates; single normalize_path; bounded-RNG helper; testkit `#[cfg(test)]` module hosting vless_empty/ctx_for/ctx_at/mux write_frame-mirror scaffolding. VLESS-encryption record-header rebuild keeps its bytes but routes 2-byte BE prefix writing through one helper.
6. Gate: `cargo test -p xray-tui-native -p xray-tui-tls` (includes ignored-e2e compile check).

### Wave 3 — app/db/tui (~1.4k LOC)

1. **Share-URL serializer reuse note**: wave 1's ShareUrlCtx already covers the app-side duplicate; nothing further here beyond migrating any importer callers surfaced by `grep -rn "format_share_url\|parse_share_url" crates/xray-tui/src`.
2. **Settings-pane accessors**: add to AppState (`state.rs`): `settings_right(&self) -> Option<&SplitRightPane>`, `settings_right_mut(&mut self) -> Option<&mut SplitRightPane>`, collapsing 43 five-line `AppMode::Settings{mode:SettingsMode::Split{..}}` matches (ui/settings.rs ×36 densest :1023-1170/:1716-2030; ops/subscriptions.rs ×7 :46,:91,:106,:151,:163,:202; ui/mod.rs scattered) to one-liners.
3. **Probe-spawn pipeline**: `ops/ping.rs` add `spawn_single_probe(state,(eid,pid),TestType, precondition:Option<&str>, probe:async fn(...))` absorbing guard-insert-spawn-result plumbing shared by start_tcp_ping/start_real_ping/start_speed_test/start_udp_test (:27,:186,:262,:322); per-decision-15 keying stays `(endpoint_id, protocol_id)`.
4. **load triage**: `ensure_loaded_protocol(db,id,log_target)->Result<Protocol,String>` in ops/, migrating 5 copies (ops/connect.rs:172-190, ops/ping.rs:198-228, ops/ping.rs:1037-1045, ops/profiles.rs:351-365, ui/mod.rs:784-792) preserving each site's failure payload shape (SpeedTestResult event vs log vs None).
5. **Form driver**: ui/settings.rs `drive_form_focus(defs,fields,focus,ev)->FormAction{Save|Cancel|Edit(usize)}` replacing the Boolean-toggle/Number-digit/Select-cycle skeleton repeated in handle_form_key(:700-858)/handle_routing_form_key(:1280-1350)/handle_group_form_key(:1875-2040) + add_server.rs form arm; Duration validation (:800) moves inside driver keyed by field kind so validation stays single-sourced.
6. **Single settings field table**: convert ui/settings.rs `form_field_defs_for_section` (:480-562) + ops/settings.rs `build_settings_fields` (:22-330) + `apply_settings_fields` (:341-530) into one const `FieldDef{key,label,kind,get:fn(&AppConfig)->String,set:fn(&mut AppConfig,&str)}` table per section; plain fn pointers, sections become data rows; ProtocolCore override branch (apply :470) and Dns empty-vs-default shape (:300-330) become FieldDef attributes.
7. **Enrichment spine**: ops/enrich.rs `emit_info_phased(entries, slow_fill)` wrapping the phase1-early-send/fill_features/phase2-resend choreography ×4 (:133-286,:270-334,:339-371,:447-500).
8. **Status derivation**: `AppState::connection_status() -> (glyph, Style, label)` consumed by actions_log.rs connection_icon(:14-24)+renderers, status_bar.rs:18-30, ui/mod.rs:1095-1105 (three current divergent glyph precedences converge on actions_log's order — Connecting > Connected > Error > Eject > Idle).
9. **Format ladders**: delete ui/profiles.rs format_traffic (:757) for `grpc_client.rs:150 format_bytes`; extract single `format_speed_bps(u64)` used by both actions_log.rs bps ladders (:85-92,:184-195).
10. **DB cascade helper**: xray-tui-db database.rs `cascade_delete_links(tx, pred)` parameterized by endpoint-id predicate, migrating purge_expired(:766-806)/delete_endpoint(:809-831)/upsert_resolved_ip_children prune arm(:682-740)/delete_group(:856-876); each still ends with purge_orphan_protocols + commit.
11. Gate: `cargo nextest run -p xray-tui -p xray-tui-db -p xray-tui-core`.

### Wave 4 — full quality gate

Run `just quality-gate` at repo root (fmt, hakari regenerate after Cargo.lock change if any dep moved, clippy pedantic+nursery, nextest, deny, machete, outdated, audit). Fix fallout wave-attributable. Per AGENTS style guide this runs once here rather than after each mechanical edit.

## Critical files & anchors

- `crates/xray-tui-proto/src/proto_spec/mod.rs` — ProtoSpec trait def :296-361, dispatch! :160, ProtocolConfig enum :404; deletions + new ctor land here
- `crates/xray-tui-proto/src/proto_spec/common.rs` — receives singbox_out_base/ShareUrlCtx/security_query_pairs; to_singbox_tls_or_default :1034 and to_singbox_transport :1074 are existing-shape models
- `crates/xray-tui-proto/src/proto_spec/kinds.rs` — enum plumbing quintets + :437 display-string pin test
- `crates/xray-tui-native/src/transport/xhttp.rs` — dialect fns :924-:1110, stream :1127, is_reality leak :655
- `crates/xray-tui/src/ui/settings.rs` — settings mode matches, form handlers, defs table :480-562

## Verification

- Per-wave gates (commands above) are the primary pins: proto round-trip suite (each protocol file carries parse↔reconstruct↔clash tests), inject_to JSON-error-shape asserts (`SupportError::MissingField("server","tuic")` in tuic.rs tests), kinds.rs `as_str_equals_protocol_display_strings`.
- New-behavior spot checks after Wave 1: `cargo nextest run -p xray-tui-proto trojan_roundtrip tuic roundtrip` style filtered runs proving a share URL reconstructed through ShareUrlCtx re-parses identically (`parse_share_url(format_share_url(parse(u)))` returns same uid); concrete input: the TUIC_URL fixture constant in tuic.rs:470 must survive format→parse→format byte-identically.
- After Wave 3: launch `cargo run`, connect any profile, confirm Profiles Test column, Settings panes, statistics screen render unchanged (manual smoke — UI lookups stay byte-compatible).
- Behavior-preservation contract throughout: identical JSON keys/error strings/wire frames/URLs before and after; audit found no clusters where consolidation intentionally changes observable output.
