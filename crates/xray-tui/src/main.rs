fn main() -> anyhow::Result<()> {
    println!("xray-tui {} — Phase 0 scaffold", env!("CARGO_PKG_VERSION"));
    // Initialize subsystems (stubs for Phase 0):
    // 1. Load app config
    // 2. Open database
    // 3. Print summary
    Ok(())
}
